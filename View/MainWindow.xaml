<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
        mc:Ignorable="d"
        Title="Hospital"  Height="450" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:HospitalViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40*"/>
            <ColumnDefinition Width="56*"/>
            <ColumnDefinition Width="140*"/>
            <ColumnDefinition Width="56*"/>
            <ColumnDefinition Width="40*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30*"/>
            <RowDefinition Height="365*"/>
            <RowDefinition Height="40*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Grid.RowSpan="2" >
            <Label Content="Введите имя:" Height="27" FontSize="14"/>
            <TextBox x:Name="NameTextBox" Height="27" Text="{Binding PatientViewModel.PatientName}" FontSize="14" />
            <Label Content="Введите фамилию:"  Height="27" FontSize="14"/>
            <TextBox x:Name="SurnameTextBox" Height="27" Text="{Binding PatientViewModel.PatientSurname}" FontSize="14"/>
            <Label Content="Выберите врача:" Height="27" FontSize="14"/>
            <ComboBox Height="27" FontSize="14" ItemsSource="{Binding DoctorModels}" SelectedItem="{Binding DoctorViewModel.SelectedDoctor}"/>
            <Label Content="Выберите время приема:" Height="27" FontSize="14"/>
            <ComboBox Height="27" FontSize="14" ItemsSource="{Binding DoctorViewModel.ListTakenTimesDoctor}" SelectedItem="{Binding AppointmentTimeViewModel.SelectedAppointmentTimeModel}"/>

        </StackPanel>
        <Button Height="27" FontSize="14" Content="Записаться на прием" Grid.Row="1" Grid.Column="2" VerticalAlignment="Bottom" Command="{Binding CreateAppointmentCommand}" IsEnabled="{Binding CanCreateAppointment}" />
    </Grid>
</Window>
