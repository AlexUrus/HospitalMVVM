<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Window.DataContext>
        <vm:HospitalViewModel/>
    </Window.DataContext>	
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="27*"/>
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0" Grid.Column="0">
			<Label Height="27" Content="Введите имя:" />
			<TextBox x:Name="NameTextBox" Height="27" Text="{Binding PatientViewModel.PatientName}">
			</TextBox>
			<Label Height="27" Content="Введите фамилию:" />
			<TextBox x:Name="SurnameTextBox" Height="27" Text="{Binding PatientViewModel.PatientSurname}">
			</TextBox>
			<Label Height="27" Content="Выберите врача:" />
			<ComboBox ItemsSource="{Binding DoctorModels}" Height="27" SelectedItem="{Binding DoctorViewModel.SelectedDoctor}">

			</ComboBox>
			<Label Height="27" Content="Выберите время приема:" />
            <ComboBox  ItemsSource="{Binding DoctorViewModel.ListTakenTimesDoctor}" Height="27" SelectedItem="{Binding AppointmentTimeViewModel.SelectedAppointmentTimeModel}">
			</ComboBox>
			<Separator Opacity="0" Height="100"/>
			<Button Content="Записаться на прием" Command="{Binding CreateAppointmentCommand}" IsEnabled="{Binding CanCreateAppointment}"/>
		</StackPanel>
	</Grid>
</Window>
